name: challenge_target_sistemas
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  webview_flutter: ^4.4.2
  shared_preferences: ^2.2.2
  mobx: ^2.2.3+1
  flutter_mobx: ^2.2.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.7
  mobx_codegen: ^2.4.1


flutter:
  uses-material-design: true
